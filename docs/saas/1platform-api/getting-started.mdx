---
title: Primeros pasos
description: Realiza tu primera llamada autenticada a la 1Platform API con el modelo de dos tokens.
sidebar_position: 2
---

# Primeros pasos

Esta guía te lleva desde cero hasta tu primera respuesta autenticada contra la **1Platform API**.

## 1. Obtén tus tokens

La 1Platform API usa un modelo de **dos tokens**:

- **Token de aplicación** (`ak-...`) — identifica tu aplicación. Se envía como `Authorization: Bearer ak-...`.
- **Token de usuario** (`sk-...`) — identifica al usuario final que actúa. Se envía como `x-user-token: sk-...`.

Las claves de aplicación se gestionan desde el panel de tu espacio de trabajo. Los tokens de usuario
se generan a través del flujo de autenticación — el recorrido completo, con el orden que hay que
respetar, está en [Autenticación](./journeys/autenticacion).

## 2. Elige un entorno

| Entorno | URL base |
|---|---|
| Producción | `https://api.1platform.pro/api/v1` |
| QA / sandbox | `https://api-qa.1platform.pro/api/v1` |

Detalles completos en [Entornos](./reference/environments).

## 3. Realiza tu primera llamada

```bash
curl https://api-qa.1platform.pro/api/v1/users/me \
  -H "Authorization: Bearer ak-your-app-token" \
  -H "x-user-token: sk-the-user-token"
```

Una respuesta exitosa usa el sobre estándar:

```json
{ "success": true, "data": { /* ... */ }, "msg": "OK" }
```

Si una llamada requiere créditos que no tienes, recibirás `402 Payment Required`; si superas un
límite de uso, `429 Too Many Requests`. Consulta [Códigos de error](./reference/error-codes)
y [Límites de uso](./reference/rate-limits).

## 4. Siguientes pasos

- **[Referencia interactiva de la API →](/api-reference/1platform-api)** — prueba cada endpoint en el navegador.
- **[Recorridos](./journeys/autenticacion)** — las integraciones con orden obligatorio, de punta a punta.
- **[Convenciones](./reference/convenciones)** — lo que vale para todas las llamadas, una sola vez.
